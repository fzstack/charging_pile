Import('RTT_ROOT')
Import('rtconfig')
from building import *

cwd = GetCurrentDir()
src = Glob('*.cxx')
CPPPATH = [
    cwd,
]

group = DefineGroup('Devices', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
